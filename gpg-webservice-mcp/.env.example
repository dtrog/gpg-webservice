# GPG Webservice MCP Adapter Configuration

# Base URL of the Flask GPG webservice
# Default: http://localhost:5000
GPG_API_BASE=http://localhost:5555

# Optional: Default API key for authenticated operations
# If not set here, clients must provide it per-request
# You can get this key by registering a user via the /openai/register_user endpoint
# GPG_API_KEY=your-api-key-here

# HTTP Transport Configuration (only used when running http-server.ts)
# Port for the HTTP MCP server
# Default: 3000
MCP_PORT=3000

# Host address to bind to
# Use 0.0.0.0 to allow external connections (for ChatGPT, etc.)
# Use 127.0.0.1 for localhost-only access
# Default: 0.0.0.0
MCP_HOST=0.0.0.0
