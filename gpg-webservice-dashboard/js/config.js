// Default configuration for local development
// In production (Render), this file is overwritten by config.template.js
// with environment variables substituted

window.GPG_CONFIG = window.GPG_CONFIG || {};
// Leave apiUrl undefined - app.js will use auto-detection for localhost
